#!/usr/bin/python3.9

#from pmodules.functionsFile.add import add #you have to specify the name of the function while importing module
from ...pmodules.functionsFile.add import add

#.pyc files are created by the python interprator when a .py file is imported into another .py file

add(2,3)